<?xml version="1.0" encoding="utf-8"?>
<!-- Modify this file to customize your launch splash screen -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fixed brand color (matches ColorScheme.fromSeed in app.dart) instead
         of plain white — see drawable-v21/launch_background.xml's comment
         for why the pre-v21 fallback should stay consistent with it. -->
    <item android:drawable="@color/launch_background_color" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher" />
    </item>
</layer-list>
